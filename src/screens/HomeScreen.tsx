import React, { FC } from 'react';
import Header from '../components/Header';

const HomeScreen: FC = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default HomeScreen;
